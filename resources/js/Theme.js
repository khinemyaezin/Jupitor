export default class Theme {
    constructor(group, type, groupTheme, articles) {
        this._group = group;
        this._type = type;
        this._groupTheme = groupTheme;
        this._articles = articles;
    }
}
